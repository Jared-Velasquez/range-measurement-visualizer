# range_measurement_visualizer
Range Measurement Visualizer: visualization of the range measurements taken by a pose.

## Getting Started

Run the script with:

```bash
cd ~/synthetic_datasets/scripts
python3 generate_datasets.py --help
```